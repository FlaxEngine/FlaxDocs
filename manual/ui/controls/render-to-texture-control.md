# Render To Texture Control

The **Render To Texture Control** control prerenders the child controls to a texture. This can be used to optimize complex GUI or controls that draw very complex shapes (eg. mesh vertices texture coordinates view).
